# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtual envs
.venv/
venv/

# Generated knowledge base is rebuilt from blog source on every deploy.
# A small placeholder copy is committed so containers (Glama, local docker build)
# can start the server for introspection without the full corpus. Do not remove
# the placeholder, but do not commit real data over it either.

# OS / editor
.DS_Store
.vscode/
.idea/

# Local secrets / env
.env
.env.local
